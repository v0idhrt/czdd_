import React from "react";
import styles from "LoginForm.module.css";

const LoginForm = () => {
    <section className={styles.LoginSection}>
        <h1 className={styles.HeaderForm}></h1>
    </section>
}